[
	VAR i int;
	SET i 5; 
/*	PROC REC tt [x:int][
		ECHO (add x i);
		SET i (sub i 1)
	];
	CALL tt 5;
	CALL tt 5;
*/
	ECHO (not(eq 0 i));
	WHILE (not(eq 0 i)) [
		ECHO i;
		SET i (sub i 1)
	];
	ECHO i
]
